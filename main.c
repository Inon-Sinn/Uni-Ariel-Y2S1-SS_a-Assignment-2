#include <stdio.h>
#include "my_mat.h"

int main(){

    char c;
    scanf("%c",&c);
    if(c=='A')
        printf("its A");
    if(c=='B')
        printf("its B");
    if(c=='C')
        printf("its C");
    if(c=='D')
        printf("its D");

    return 0;
}